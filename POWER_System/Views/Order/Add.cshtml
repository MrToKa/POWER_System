@model PartOrderServiceModel

@{
    ViewBag.Title = "Add Parts order";
}

<h2 class="text-center">@ViewBag.Title</h2>

<div class="row">
    <div class="col-sm-12 offset-lg-2 col-lg-8 offset-xl-3 col-xl-6">
        <form method="post" asp-action="Add" asp-controller="Order" asp-route-id="@Model.Id">
            @*<div class="mb-3">
                <input type="hidden" asp-for="@Model.ProjectId" class="form-control" value="@Context.GetRouteData().Values["id"]" />
            </div>*@
            <div class="mb-3">
                <input type="hidden" asp-for="@Model.EnclosureId" class="form-control" value="@Model.EnclosureId" />
            </div>            
            @*<div class="mb-3">
                <input type="hidden" asp-for="@Model.Project" class="form-control" value="@Model.Project" />
            </div>
            <div class="mb-3">
                <input type="hidden" asp-for="@Model.EnclosureId" class="form-control" value="@Model.Enclosure" />
            </div>*@
            <div class="mb-3">
                <label asp-for="@Model.OrderDate" class="form-label">Date</label>
                <input asp-for="@Model.OrderDate" class="form-control" ariarequired="true" type="date" value="DateOnly" />
                @*<span asp-validation-for="OrderDate" class="text-danger"></span>*@
            </div>
            <div class="mb-3">
                <label asp-for="@Model.Comment" class="form-label">Comment</label>
                <input asp-for="@Model.Comment" class="form-control" value="" />
                @*<span asp-validation-for="Comment" class="text-danger"></span>*@
            </div>
            <div class="mb-3">
                <input class="btn btn-primary" type="submit" value="Add Part order for Enclosure/Cabinet"  />
            </div>
        </form>
    </div>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}